<h3> Edit Product </h3>

<%= form_for([:admin, @product]) do |f| %>
  <%= render partial: 'form', locals: { f: f }%>
    <div class="mb-3">
        <%= f.submit 'Update Product', class: 'btn btn-primary' %>
    </div>
<% end %>

<%= link_to "Back", admin_products_path, class: "card-link" %>